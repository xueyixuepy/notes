# 记录
-   题目:给你一个长度为 n 的链表，每个节点包含一个额外增加的随机指针 random ，该指针可以指向链表中的任何节点或空节点。
    构造这个链表的 深拷贝。 深拷贝应该正好由 n 个 全新 节点组成，其中每个新节点的值都设为其对应的原节点的值。新节点的 next 指针和 random 指针也都应指向复制链表中的新节点，并使原链表和复制链表中的这些指针能够表示相同的链表状态。复制链表中的指针都不应指向原链表中的节点 。

    例如，如果原链表中有 X 和 Y 两个节点，其中 X.random --> Y 。那么在复制链表中对应的两个节点 x 和 y ，同样有 x.random --> y 。

    返回复制链表的头节点。

    用一个由 n 个节点组成的链表来表示输入/输出中的链表。每个节点用一个 [val, random_index] 表示：

    val：一个表示 Node.val 的整数。
    random_index：随机指针指向的节点索引（范围从 0 到 n-1）；如果不指向任何节点，则为  null 。
    你的代码 只 接受原链表的头节点 head 作为传入参数。
- 示例:
  输入：head = [[7,null],[13,0],[11,4],[10,2],[1,0]]
  输出：[[7,null],[13,0],[11,4],[10,2],[1,0]]
- 思路:我用暴力方法,记录原链表的排序情况以及random对应的位置,然后再按顺序生成新链表,逐个修改random值.
```cpp
/*
// Definition for a Node.
class Node {
public:
    int val;
    Node* next;
    Node* random;
    
    Node(int _val) {
        val = _val;
        next = NULL;
        random = NULL;
    }
};
*/

class Solution {
public:
    Node* copyRandomList(Node* head) {
        map<Node* ,int> m;
        map<int,Node* > m2;

        Node* tem = head;
        Node* pre = new Node(0);
        Node* preHead = pre;
        Node* now;
        int cout = 0;
        while(tem!=nullptr){
           
            now = new Node(tem->val);
            pre->next = now;
            pre = now;
            m[tem] = cout;
            cout++;
            tem = tem->next;
        }

        Node* newHead = preHead->next;
        tem = head;
        vector<int> ck;
        m[NULL] = -1;
        while(tem!=nullptr){
            ck.emplace_back(m[tem->random]);
            tem = tem->next;
        }
        tem = newHead;
        cout=0;
        while(tem!=nullptr){
            m2[cout] = tem;
            cout++;
            tem = tem->next;
        }
        m2[-1]=NULL;
        cout=0;
        tem = newHead;
        while(tem!=nullptr){
            tem->random = m2[ck[cout]];
            cout++;
            tem = tem->next;
        }
        return newHead;
    }
};
```
- 结果:改了两个语法错误就通过了,但是时间空间都比较差.
- 他人的解法
```js
var copyRandomList = function (head) {
    // 旧零件 → 新的裸零件
    const map = new Map()

    // —— 1 · 卸骨 ——
    // 拆旧零件；记住构造，拔掉所有线
    let old = head
    while (old) {
        map.set(old, new _Node(old.val, null, null))
        old = old.next
    }

    // —— 2 · 组装 ——
    // 照着旧壳组装
    let p = head
    while (p) {
        const newNode = map.get(p)
        newNode.next = map.get(p.next) || null
        newNode.random = map.get(p.random) || null

        p = p.next
    }

    // 新东风
    return map.get(head)
};
```
- 解析:也是用了字典,我想多了,==做映射只需要一个字典就够了==,而不需要那么复杂.